# 📦 Подробная инструкция по установке ДоброБота

## 🖥️ Требования к системе

### Windows
- Windows 7 и выше (рекомендуется Windows 10+)
- Python 3.9+ (скачать с [python.org](https://www.python.org/downloads/))
- 100 МБ свободного места на диске

### macOS
- macOS 10.9 и выше
- Python 3.9+ (установить через [Homebrew](https://brew.sh/): `brew install python3`)
- 100 МБ свободного места на диске

### Linux
- Python 3.9+
- pip (обычно идет с Python)
- 100 МБ свободного места на диске

## ✅ Проверка установки Python

```bash
# Проверяем версию Python
python --version
# или
python3 --version

# Должно вывести версию 3.9 или выше
```

## 📥 Пошаговая установка

### Шаг 1️⃣: Клонируем репозиторий

```bash
git clone https://github.com/your-username/dobrobot.git
cd dobrobot
```

Или скачиваем как ZIP архив и распаковываем.

### Шаг 2️⃣: Создаем виртуальное окружение

**На Windows:**
```bash
# Создаем виртуальное окружение
python -m venv venv

# Активируем его
venv\Scripts\activate

# Если выше не сработало, попробуйте:
python -m venv venv
.\venv\Scripts\activate
```

**На macOS/Linux:**
```bash
# Создаем виртуальное окружение
python3 -m venv venv

# Активируем его
source venv/bin/activate
```

### Шаг 3️⃣: Устанавливаем зависимости

```bash
# Обновляем pip
pip install --upgrade pip

# Устанавливаем зависимости
pip install -r requirements.txt
```

### Шаг 4️⃣: Получаем Telegram Bot Token

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/start`
3. Отправьте команду `/newbot`
4. Следуйте инструкциям и скопируйте полученный токен

### Шаг 5️⃣: Конфигурируем .env файл

```bash
# Создаем .env файл из примера
cp .env.example .env

# Или вручную создаем файл .env и добавляем:
```

**Содержание .env:**
